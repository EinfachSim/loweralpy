# loweralpy
Your interpreter for LowerAlpha code!

To run, simply run lasm.py in your Terminal and provide a .lasm file containing a valid LowerAlpha-Program


  `python lasm.py <your .lasm file>`
  
Currently NOT supporting stack operations (stack op where op in {+, -, *, /, %})!
